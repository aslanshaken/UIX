import React from 'react';
import { Routes, Route, useNavigate } from 'react-router-dom';

const Routing = () => {

  return (
    <Routes>
      <Route path="/" />
      <Route path="/why-us" />
    </Routes>
  );
};

export default Routing;
